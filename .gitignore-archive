# 工作空间归档 - 2026-03-29
# 包含：NexusAI Tech (my-project), RegimeTrader AI (regime_trader_ai_product), 及相关文档与脚本
# 排除：大型数据文件（*.pkl, *.csv, *.hdf5, *.parquet, *.zip）、虚拟环境、node_modules、dist

# 大型数据文件（已备份，无需重复）
*.pkl
*.csv
*.hdf5
*.h5
*.parquet
*.zip
*.tar.gz

# 虚拟环境与依赖
.venv/
venv/
node_modules/
__pycache__/

# 构建输出
dist/
build/
*.log

# 操作系统文件
.DS_Store
Thumbs.db

# 安全密钥（生产环境请自行配置）
.env
*secret*
*password*
*.pem
*.key

# 临时文件与缓存
tmp/
temp/
.cache/
logs/

# 大型二进制/安装包
WeChat.deb
packages-microsoft-prod.deb
shrimp_flash_install.ps1
china_install.ps1
install-dotnet-windows.ps1

# 下面这些是重要项目，但排除其子目录中的大数据
/my-project/frontend/dist/
/my-project/frontend/node_modules/
/regime_trader_ai_product/data/
/regime_trader_ai_product/*.pkl
/regime_trader_ai_product/regime_model.pkl
/regime_trader_ai_product/*.tar.gz
/regime_trader_ai_product/*.zip

# 其他可选的临时归档（已存在于其他仓库）
my-project/.git/
my-project/.idea/
my-project/.vscode/
my-project/*.sln
my-project/*.csproj
my-project/Properties/
my-project/obj/
