# Memory Log - 2026-03-22

## 📅 Mar 22, 2026 (Today)

### 🎯 Major Milestone: NexusAI Tech MVP Complete

**Status:** Phase 1 MVP development finished and ready for integration testing.

#### Backend (C# / .NET 10)
- API running on `http://localhost:7092` (SQLite dev database)
- JWT authentication with phone verification code (dev: "123456")
- All core endpoints operational:
  - `/api/auth/send-code` & `/api/auth/phone-login`
  - `/api/seller/profile`
  - `/api/support/dashboard`
  - `/api/support/agents/stats`
  - `/api/support/tickets`
  - `/api/support/tickets/{id}/messages`
  - `/api/support/tickets/{id}/reply`
  - `/api/auth/init-agent` (test agent creation)

#### Frontend (uni-app Vue3)
- All pages rebuilt with real API integration
- Login page: phone verification + token storage
- Dashboard (`/pages/dashboard/dashboard.vue`): displays seller profile, quota, subscription
- Support Workbench (`/pages/support/workbench.vue`): three-panel layout (tickets list, chat, customer info)
- Supervisor Panel (`/pages/support/supervisor.vue`): session stats + agent performance
- Build successful, ready for HBuilderX deployment

#### Integration Ready
- Use tel: "13800138000", verification code: "123456"
- Token auto-saved to local storage; role-based access control implemented

---

### 📈 RegimeTrader AI v2 Performance

**System:** live_executor.py running continuously (PID 788183)
**Current Position:** XRP/USDT long
**Recent Performance:**
- 06:05 UTC: Equity $10,076.42 (6 trades, 20% win rate, net P&L: -$159.80)
- Position floating profit: +$274.52
- Model: regime_model_v2_multi_full.pkl (multi-symbol trained)
- Parameters: ADX>=23, CONFIDENCE>=0.60, LEVERAGE=2.5x, RISK=5%

Notes: Strategy scanning top 60 symbols, only trades 5 whitelisted training pairs (BTC, ETH, BNB, SOL, XRP). No new entries recently due to trend threshold filters.

---

### 🗂️ File Locations

- Backend: `my-project/Synerixis.Api/`
- Frontend: `my-project/frontend/`
- DB: `my-project/Synerixis.Api/bin/Debug/net10.0/dev.db`
- v2 logs: `regime_trader_ai_product/logs/v2_trader.log`
- v2 state: `regime_trader_ai_product/paper_trade_state_v2.json`

---

### ⏭️ Next Steps

1. **Boss integration testing:** Full end-to-end test of login → dashboard → workbench → supervisor flows.
2. **Production upgrade:** Replace SQLite with MySQL (update `appsettings.json` provider).
3. **Agent onboarding:** Implement agent login UI and role assignment.
4. **Chat AI integration:** Connect conversation service to AI backend (Semantic Kernel).
5. **Performance review:** After 20-30 closed trades, evaluate v2 metrics for parameter tuning.

---

### 🔧 Technical Notes

- Temporary dev SQLite provider hard-coded in `Program.cs` - remember to revert to MySQL config before production.
- Phone login bypasses verification check in Development (`_env.IsDevelopment()`).
- All frontend API calls use unified `request.js` wrapper (BASE_URL = `http://localhost:7092`).
- v1 paper trader stopped; only v2 remains active.
